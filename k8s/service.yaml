apiVersion: v1
kind: Service
metadata:
  name: fastapi-app-svc
  namespace: default
  labels:
    app: fastapi-app
spec:
  selector:
    app: fastapi-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
  type: ClusterIP
